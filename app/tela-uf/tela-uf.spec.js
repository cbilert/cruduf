'use strict';

describe('myApp.tela-uf module', function() {

  beforeEach(module('myApp.tela-uf'));

  describe('tela-uf controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var viewCtrl = $controller('TelaUFCtrl');
      expect(viewCtrl).toBeDefined();
    }));

  });
});